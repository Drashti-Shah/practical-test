import { Martel_Sans, Open_Sans } from "next/font/google";

export const martelSans = Martel_Sans({
  subsets: ["latin"],
  weight: ["400", "600", "700"],
});

export const openSans = Open_Sans({
  subsets: ["latin"],
  weight: ["400", "600", "700"],
});
